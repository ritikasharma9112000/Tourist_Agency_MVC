@model IEnumerable<Tourist_Agency_MVC.Models.Activities>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Activity_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activity_Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activity_Available_Season)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activity_Special_Need)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Activity_Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Activity_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity_Duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity_Available_Season)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity_Special_Need)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity_Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
